import { TYPES } from '../actions'

const initialStates = {
}


export const catalogueData = (state = initialStates, action) => {
    switch (action.type) {
        default:
            return state
    }
}
